# 操作记录

本文件记录每次实际操作、命令输出、遇到的问题和解决过程。

## 第一次记录

- 当前状态：项目目录已创建。
- 当前状态：中文阶段文档目录已建立。
- 当前状态：已创建功能分支 `feat/first-python-program`。
- 当前状态：已确认 Python 版本为 3.12.10，pytest 版本为 8.3.3。
- 当前状态：先编写启动测试，确认测试因缺少 `relaytown` 包而失败。
- 当前状态：创建 `relaytown` 包和 `__main__.py`，测试通过。
- 当前状态：使用 `python -X utf8 -m relaytown` 手动启动，看到 `RelayTown 正在启动`。
- 当前状态：增加 Python 项目的 `.gitignore`。
- 下一步：提交本次最小 Python 启动版本，并回顾每个文件的作用。
